/* 
    crie um programa que peça ao usuario um valor inicial e um valor
    final para gerar tabuadas do valor inicial até o final.
*/

let entrada = require('prompt-sync')();

let vinicial = (entrada("Digite um valor inicial: "));
let vfinal = (entrada("Digite um valor final: "));
let result = 0;
contador = 0;
